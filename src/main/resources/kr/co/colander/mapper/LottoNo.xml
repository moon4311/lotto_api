<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.colander.mapper.LottoNo">
 
 	<sql id="tblnm">LOTTO_NO_TB</sql>
 	
 	<!-- 다건 조회 -->
 	<select id="selectList" parameterType="map" resultType="LottoNoVO">
		SELECT 	LOTTO_NO,
						BALL_NO1,
						BALL_NO2,
						BALL_NO3,
						BALL_NO4,
						BALL_NO5,
						BALL_NO6
		FROM 		<include refid="tblnm" />
		LIMIT 10
	</select>
	
	<!-- 입력 -->
	<insert id="insert" parameterType="map">
		INSERT INTO <include refid="tblnm"/>
		(
			BALL_NO1,
			BALL_NO2,
			BALL_NO3,
			BALL_NO4,
			BALL_NO5,
			BALL_NO6,
			sum
		)
		values
		(
			#{ballNo1},
			#{ballNo2},
			#{ballNo3},
			#{ballNo4},
			#{ballNo5},
			#{ballNo6},
			#{sum}
		)
	</insert>
</mapper>